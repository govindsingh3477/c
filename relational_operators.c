#include <stdio.h>
int main()
{
    int p = 5, q = 7;
    printf("p > q: %d \n", p > q);
    printf("p >= q: %d \n", p >= q);
    printf("p <= q: %d \n", p <= q);
    printf("p < q: %d \n", p < q);
    printf("p == q: %d \n", p == q);
    printf("p != q: %d \n", p != q);
    return 0;
}
